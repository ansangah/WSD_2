이 파일은 제출 시 실제 값을 채워 Classroom에 업로드합니다. Public repo에는 템플릿만 유지하세요.

1. DB 연결 정보 (PostgreSQL)
   - Host:
   - Port:
   - Database:
   - Username:
   - Password:
   - 접속 명령어 예시: `psql postgresql://<user>:<password>@<host>:<port>/<database>`

2. JCloud 인스턴스
   - Public IP / 도메인:
   - SSH 사용자명:
   - PM2 프로세스 이름:

3. 기타 사용 ID/비밀번호
   - 관리자 계정: `admin@example.com / P@ssw0rd!`
   - 매니저 계정: `manager@example.com / P@ssw0rd!`
   - 일반 계정 예시: `user1@example.com / P@ssw0rd!`

※ 실 비밀번호/키 파일은 `.env` 및 Classroom 제출물에만 포함하세요.
